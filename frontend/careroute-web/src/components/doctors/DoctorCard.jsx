import PropTypes from 'prop-types';import{Link}from'react-router-dom';import{labelConstant}from'../../utils/domain.js'
export default function DoctorCard({doctor}){return <article className="card"><h2><Link to={`/doctors/${doctor.id}`}>{doctor.name}</Link></h2><p>{labelConstant(doctor.specialization)}</p>{doctor.qualification&&<p>{doctor.qualification}</p>}<p>Languages: {doctor.supportedLanguages.join(', ')}</p><p>Facilities: {doctor.facilities.map(item=>item.name).join(', ')||'None listed'}</p><p>Services: {doctor.healthServices.map(item=>item.name).join(', ')||'None listed'}</p></article>};DoctorCard.propTypes={doctor:PropTypes.object.isRequired}
