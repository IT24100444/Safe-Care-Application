import PropTypes from 'prop-types'; import { Link } from 'react-router-dom'
export default function FacilityCard({ facility }) { return <article className="resource-card"><h2><Link to={`/facilities/${facility.id}`}>{facility.name}</Link></h2><p>{facility.facilityType.replaceAll('_', ' ')} · {facility.district}</p><p>{facility.address}</p><p>Languages: {facility.supportedLanguages.join(', ')}</p><p>{facility.emergencyAvailable ? 'Emergency capability listed' : 'Emergency capability not listed'}</p><p>Services offered: {facility.healthServices?.map((item) => item.name).join(', ') || 'None listed'}</p></article> }
FacilityCard.propTypes = { facility: PropTypes.shape({ id: PropTypes.string.isRequired, name: PropTypes.string.isRequired, facilityType: PropTypes.string.isRequired, district: PropTypes.string.isRequired, address: PropTypes.string.isRequired, supportedLanguages: PropTypes.arrayOf(PropTypes.string).isRequired, emergencyAvailable: PropTypes.bool.isRequired, healthServices: PropTypes.arrayOf(PropTypes.object) }).isRequired }
