import { useEffect, useState } from 'react'; import { getHealthServices } from '../features/services/healthServiceService.js'
export default function ServicesPage() { const [data, setData] = useState(null); const [error, setError] = useState(''); useEffect(() => { getHealthServices().then(setData).catch(() => setError('Unable to load health services.')) }, []); return <section><h1>Health services</h1>{error ? <p role="alert">{error}</p> : !data ? <p role="status">Loading health services…</p> : data.count === 0 ? <p>No active health services found.</p> : <div className="cards">{data.items.map((item) => <article className="resource-card" key={item.id}><h2>{item.name}</h2><p>{item.category}</p>{item.description && <p>{item.description}</p>}</article>)}</div>}</section> }
