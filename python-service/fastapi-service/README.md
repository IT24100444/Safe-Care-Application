# CareRoute LK Python Service

Small non-AI FastAPI foundation for future Python functionality. From this directory, create a virtual environment, install `requirements.txt`, then run `uvicorn app.main:app --reload --port 8000`.
